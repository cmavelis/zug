import { test, expect } from 'vitest';
import {
  arrayToCoordinates,
  coordinatesToArray,
  isOppositeVector,
  stripSecrets,
} from '@/game/common';
import { makeTestGame } from '@/game/test-utils';

test('arrange orders, basic', () => {});
